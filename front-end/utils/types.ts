import { ReactNode } from "react";

export interface navBarTypes {
  name: string;
  icon: ReactNode;
}

export interface CardType {
  image: string;
  type: string;
  title: string;
  desc: string;
}
